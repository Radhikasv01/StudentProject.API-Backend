{
	"buildCommand": "dotnet publish StudentProject.API/StudentProject.API.csproj -c Release -o publish",
	"outputDirectory": "publish",
	"routes": [
		{
			"src": "/(.*)",
			"dest": "publish/$1"
		}
	]
}